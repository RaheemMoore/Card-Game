# Task for Claude

Please inspect this entire `halo-stone-castle-kit-2026-08-05` folder and organize it into the
repository's new asset structure without deleting or overwriting the handoff.

Use `castle-kit-manifest.json` as the inventory and `HANDOFF_NOTES.md` as the technical contract.

Before moving anything:

1. Report the destination folders you propose.
2. Keep `review/` and `archive/proofs/` out of runtime registration.
3. Preserve stable asset IDs and readable kebab-case filenames.
4. Do not register assets marked `NEEDS ADJUSTMENT`, `NEEDS CLEANUP`, or `NEEDS SEPARATION` as production-ready.
5. Do not replace the current live `/castle` scene.
6. Coordinate any Phaser Editor project boundary with Codex after organization is complete.

The immediate goal is an organized, reversible asset import—not production scene integration.
